<%= simple_form_for(@usuario) do |f| %>
  <%= f.input :nome, error: 'Campo obrigatório!' %>
  <%= f.input :matricula, error: 'Campo obrigatório!' %>
  <%= f.button :submit, 'Enviar', class: 'btn-primary' %>
<% end %>

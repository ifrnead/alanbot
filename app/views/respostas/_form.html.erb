<%= simple_form_for(@resposta) do |f| %>
  <%= f.input :mensagem, error: 'Campo obrigatório!' %>
  <%= f.input :alvo_id, collection: Alvo.select, prompt: "Escolha um alvo" %>
  <%= f.input :informacao_id, collection: Informacao.select(label: 'tipo'), prompt: "Escolha uma informacao" %>
  <%= f.button :submit, 'Enviar', class: 'btn-primary' %>
<% end %>


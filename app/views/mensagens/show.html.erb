<div class="row">
  <div class="col-sm">
    <h1><%= fa_icon 'comment' %> Mensagem</h1>
  </div>
  <div class="col-sm text-right">
    <div class="btn-group" role="group" aria-label="Opções">
      <%= link_to fa_icon('undo', text: 'Voltar'), mensagens_path, class: "btn btn-info" %>
    </div>
  </div>
</div>

<dl>
  <dt>Texto</dt><dd><%= @mensagem.texto %></dd>
  <dt>Resposta</dt>
  <dd>
    <% if @mensagem.resposta %>
      <%= @mensagem.resposta.mensagem %>
    <% else %>
      Nenhuma resposta encontrada.
    <% end %>
  </dd>
  <dt>Json</dt><dd><%= @mensagem.json %></dd>
</dl>
